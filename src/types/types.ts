export interface Car {
    id: number;
    brand: string;
    rating: number;
    carName: string;
    imgUrl: string;
    model: string;
    price: number;
    speed: string;
    gps: string;
    seatType: string;
    automatic: string;
    description: string;
    name?: string; // Optional
    mileage?: string; // Optional
    transmission?: string; // Optional
  }
  